{% extends 'base.html.twig' %}
{% block title %}Editer Apprenant{% endblock %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Modifier</button>

    {{ form_end(form) }}




    <a href= "{{path('apprenant_list')  }}">Retour</a>

{% endblock %}
